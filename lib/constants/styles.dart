import 'package:flutter/cupertino.dart';

class Styles{
  Styles._();
  static const EdgeInsetsGeometry padding = EdgeInsets.all(16);
  static const EdgeInsetsGeometry descriptionPadding = EdgeInsets.symmetric(horizontal: 40, vertical: 10);
  static const EdgeInsetsGeometry horizontalPadding = EdgeInsets.symmetric(horizontal: 16);
static const ShapeBorder borderRadius = RoundedRectangleBorder(borderRadius: BorderRadius.all(Radius.circular(10)));

}