import 'package:flutter/material.dart';

class ColorPalette{
  ColorPalette._();
  static const Color primaryColor = Color(0xFF141441);
  static const Color secondaryColor = Color(0xFFfed25f);
  static  Color blurColor = Colors.white.withOpacity(0.1);
}